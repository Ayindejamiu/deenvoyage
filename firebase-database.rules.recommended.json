{
  "rules": {
    ".read": false,
    ".write": false,

    "public": {
      ".read": true,
      ".write": false
    },

    "registrations": {
      ".read": false,
      ".write": "auth != null",
      "$registrationId": {
        ".validate": "newData.hasChildren(['fname','email','travelType','createdAt']) && newData.child('fname').isString() && newData.child('fname').val().length <= 100 && newData.child('email').isString() && newData.child('email').val().length <= 200 && newData.child('travelType').isString() && newData.child('travelType').val().length <= 50 && newData.child('createdAt').isNumber() && newData.child('createdAt').val() <= now && newData.child('createdAt').val() > now - 1000 * 60 * 60 * 24"
      }
    }
  }
}
